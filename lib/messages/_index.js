// eslint-disable
import { getLocale, trackMessageCall, experimentalMiddlewareLocaleSplitting, isServer } from "../runtime.js";
import * as en from "./en.js";
import * as fr from "./fr.js";
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{ username: NonNullable<unknown> }} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const example_message = (inputs, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.example_message(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("example_message", locale);
    if (locale === "en")
        return en.example_message(inputs);
    return fr.example_message(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const never_leave_hiring_young_tech_talent_to_chance = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.never_leave_hiring_young_tech_talent_to_chance(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("never_leave_hiring_young_tech_talent_to_chance", locale);
    if (locale === "en")
        return en.never_leave_hiring_young_tech_talent_to_chance(inputs);
    return fr.never_leave_hiring_young_tech_talent_to_chance(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const get_started = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.get_started(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("get_started", locale);
    if (locale === "en")
        return en.get_started(inputs);
    return fr.get_started(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const start_free = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.start_free(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("start_free", locale);
    if (locale === "en")
        return en.start_free(inputs);
    return fr.start_free(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const stop_filtering_thousands_of_applications_manually = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.stop_filtering_thousands_of_applications_manually(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("stop_filtering_thousands_of_applications_manually", locale);
    if (locale === "en")
        return en.stop_filtering_thousands_of_applications_manually(inputs);
    return fr.stop_filtering_thousands_of_applications_manually(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const powerful_features_for_modern_recruiting_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.powerful_features_for_modern_recruiting_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("powerful_features_for_modern_recruiting_teams", locale);
    if (locale === "en")
        return en.powerful_features_for_modern_recruiting_teams(inputs);
    return fr.powerful_features_for_modern_recruiting_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const we_ve_built_a_suite_of_powerful_features_that_makes_recruiting = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.we_ve_built_a_suite_of_powerful_features_that_makes_recruiting(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("we_ve_built_a_suite_of_powerful_features_that_makes_recruiting", locale);
    if (locale === "en")
        return en.we_ve_built_a_suite_of_powerful_features_that_makes_recruiting(inputs);
    return fr.we_ve_built_a_suite_of_powerful_features_that_makes_recruiting(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const technical_assessment = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.technical_assessment(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("technical_assessment", locale);
    if (locale === "en")
        return en.technical_assessment(inputs);
    return fr.technical_assessment(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const automated_github_analysis = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.automated_github_analysis(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("automated_github_analysis", locale);
    if (locale === "en")
        return en.automated_github_analysis(inputs);
    return fr.automated_github_analysis(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const instantly_analyze_candidates_github_profiles1 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.instantly_analyze_candidates_github_profiles1(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("instantly_analyze_candidates_github_profiles1", locale);
    if (locale === "en")
        return en.instantly_analyze_candidates_github_profiles1(inputs);
    return fr.instantly_analyze_candidates_github_profiles1(inputs);
};
export { instantly_analyze_candidates_github_profiles1 as "instantly_analyze_candidates_gitHub_profiles" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const smart_ranking = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.smart_ranking(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("smart_ranking", locale);
    if (locale === "en")
        return en.smart_ranking(inputs);
    return fr.smart_ranking(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const ai_powered_candidate_ranking = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.ai_powered_candidate_ranking(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("ai_powered_candidate_ranking", locale);
    if (locale === "en")
        return en.ai_powered_candidate_ranking(inputs);
    return fr.ai_powered_candidate_ranking(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const our_ai_analyzes_multiple_data_points_to_rank_candidates = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.our_ai_analyzes_multiple_data_points_to_rank_candidates(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("our_ai_analyzes_multiple_data_points_to_rank_candidates", locale);
    if (locale === "en")
        return en.our_ai_analyzes_multiple_data_points_to_rank_candidates(inputs);
    return fr.our_ai_analyzes_multiple_data_points_to_rank_candidates(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const trusted_by_leading_tech_companies = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.trusted_by_leading_tech_companies(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("trusted_by_leading_tech_companies", locale);
    if (locale === "en")
        return en.trusted_by_leading_tech_companies(inputs);
    return fr.trusted_by_leading_tech_companies(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const simple_transparent_pricing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.simple_transparent_pricing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("simple_transparent_pricing", locale);
    if (locale === "en")
        return en.simple_transparent_pricing(inputs);
    return fr.simple_transparent_pricing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const select_your_company_size_to_see_pricing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.select_your_company_size_to_see_pricing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("select_your_company_size_to_see_pricing", locale);
    if (locale === "en")
        return en.select_your_company_size_to_see_pricing(inputs);
    return fr.select_your_company_size_to_see_pricing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const monthly_billing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.monthly_billing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("monthly_billing", locale);
    if (locale === "en")
        return en.monthly_billing(inputs);
    return fr.monthly_billing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const annual_billing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.annual_billing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("annual_billing", locale);
    if (locale === "en")
        return en.annual_billing(inputs);
    return fr.annual_billing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const save_20_percent = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.save_20_percent(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("save_20_percent", locale);
    if (locale === "en")
        return en.save_20_percent(inputs);
    return fr.save_20_percent(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const employees = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.employees(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("employees", locale);
    if (locale === "en")
        return en.employees(inputs);
    return fr.employees(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const small_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.small_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("small_teams", locale);
    if (locale === "en")
        return en.small_teams(inputs);
    return fr.small_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const empowering_for_startups_and_small_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.empowering_for_startups_and_small_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("empowering_for_startups_and_small_teams", locale);
    if (locale === "en")
        return en.empowering_for_startups_and_small_teams(inputs);
    return fr.empowering_for_startups_and_small_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const for_teams_up_to_10_employees = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.for_teams_up_to_10_employees(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("for_teams_up_to_10_employees", locale);
    if (locale === "en")
        return en.for_teams_up_to_10_employees(inputs);
    return fr.for_teams_up_to_10_employees(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const up_to_100_candidates = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.up_to_100_candidates(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("up_to_100_candidates", locale);
    if (locale === "en")
        return en.up_to_100_candidates(inputs);
    return fr.up_to_100_candidates(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const basic_analytics = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.basic_analytics(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("basic_analytics", locale);
    if (locale === "en")
        return en.basic_analytics(inputs);
    return fr.basic_analytics(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const email_support = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.email_support(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("email_support", locale);
    if (locale === "en")
        return en.email_support(inputs);
    return fr.email_support(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const get_started_free = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.get_started_free(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("get_started_free", locale);
    if (locale === "en")
        return en.get_started_free(inputs);
    return fr.get_started_free(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const unlimited_candidates = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.unlimited_candidates(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("unlimited_candidates", locale);
    if (locale === "en")
        return en.unlimited_candidates(inputs);
    return fr.unlimited_candidates(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const enhanced_github_hackathon_analytics1 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.enhanced_github_hackathon_analytics1(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("enhanced_github_hackathon_analytics1", locale);
    if (locale === "en")
        return en.enhanced_github_hackathon_analytics1(inputs);
    return fr.enhanced_github_hackathon_analytics1(inputs);
};
export { enhanced_github_hackathon_analytics1 as "enhanced_gitHub_hackathon_analytics" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const priority_email_chat_support = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.priority_email_chat_support(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("priority_email_chat_support", locale);
    if (locale === "en")
        return en.priority_email_chat_support(inputs);
    return fr.priority_email_chat_support(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const save = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.save(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("save", locale);
    if (locale === "en")
        return en.save(inputs);
    return fr.save(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const a_year = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.a_year(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("a_year", locale);
    if (locale === "en")
        return en.a_year(inputs);
    return fr.a_year(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const for_large_organizations_with_custom_requirements = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.for_large_organizations_with_custom_requirements(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("for_large_organizations_with_custom_requirements", locale);
    if (locale === "en")
        return en.for_large_organizations_with_custom_requirements(inputs);
    return fr.for_large_organizations_with_custom_requirements(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const custom = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.custom(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("custom", locale);
    if (locale === "en")
        return en.custom(inputs);
    return fr.custom(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const contact_sales_for_pricing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.contact_sales_for_pricing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("contact_sales_for_pricing", locale);
    if (locale === "en")
        return en.contact_sales_for_pricing(inputs);
    return fr.contact_sales_for_pricing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const full_ats_suite_integration = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.full_ats_suite_integration(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("full_ats_suite_integration", locale);
    if (locale === "en")
        return en.full_ats_suite_integration(inputs);
    return fr.full_ats_suite_integration(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const dedicated_support = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.dedicated_support(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("dedicated_support", locale);
    if (locale === "en")
        return en.dedicated_support(inputs);
    return fr.dedicated_support(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const contact_sales = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.contact_sales(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("contact_sales", locale);
    if (locale === "en")
        return en.contact_sales(inputs);
    return fr.contact_sales(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const free = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.free(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("free", locale);
    if (locale === "en")
        return en.free(inputs);
    return fr.free(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const employee_per_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.employee_per_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("employee_per_month", locale);
    if (locale === "en")
        return en.employee_per_month(inputs);
    return fr.employee_per_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const start_for_free = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.start_for_free(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("start_for_free", locale);
    if (locale === "en")
        return en.start_for_free(inputs);
    return fr.start_for_free(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const medium_sized_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.medium_sized_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("medium_sized_teams", locale);
    if (locale === "en")
        return en.medium_sized_teams(inputs);
    return fr.medium_sized_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const up_to_9 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.up_to_9(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("up_to_9", locale);
    if (locale === "en")
        return en.up_to_9(inputs);
    return fr.up_to_9(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const success_at_a_glance = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.success_at_a_glance(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("success_at_a_glance", locale);
    if (locale === "en")
        return en.success_at_a_glance(inputs);
    return fr.success_at_a_glance(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const track_your_hiring_success_with_powerful_analytics = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.track_your_hiring_success_with_powerful_analytics(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("track_your_hiring_success_with_powerful_analytics", locale);
    if (locale === "en")
        return en.track_your_hiring_success_with_powerful_analytics(inputs);
    return fr.track_your_hiring_success_with_powerful_analytics(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const total_applications = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.total_applications(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("total_applications", locale);
    if (locale === "en")
        return en.total_applications(inputs);
    return fr.total_applications(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const plus_20_last_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.plus_20_last_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("plus_20_last_month", locale);
    if (locale === "en")
        return en.plus_20_last_month(inputs);
    return fr.plus_20_last_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const qualified_candidates = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.qualified_candidates(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("qualified_candidates", locale);
    if (locale === "en")
        return en.qualified_candidates(inputs);
    return fr.qualified_candidates(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const plus_12_from_last_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.plus_12_from_last_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("plus_12_from_last_month", locale);
    if (locale === "en")
        return en.plus_12_from_last_month(inputs);
    return fr.plus_12_from_last_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const time_to_hire = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.time_to_hire(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("time_to_hire", locale);
    if (locale === "en")
        return en.time_to_hire(inputs);
    return fr.time_to_hire(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const less_30_from_last_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.less_30_from_last_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("less_30_from_last_month", locale);
    if (locale === "en")
        return en.less_30_from_last_month(inputs);
    return fr.less_30_from_last_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const big_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.big_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("big_teams", locale);
    if (locale === "en")
        return en.big_teams(inputs);
    return fr.big_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const empowering_for_large_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.empowering_for_large_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("empowering_for_large_teams", locale);
    if (locale === "en")
        return en.empowering_for_large_teams(inputs);
    return fr.empowering_for_large_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const platform = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.platform(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("platform", locale);
    if (locale === "en")
        return en.platform(inputs);
    return fr.platform(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const toggle = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.toggle(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("toggle", locale);
    if (locale === "en")
        return en.toggle(inputs);
    return fr.toggle(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const projects = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.projects(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("projects", locale);
    if (locale === "en")
        return en.projects(inputs);
    return fr.projects(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const more = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.more(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("more", locale);
    if (locale === "en")
        return en.more(inputs);
    return fr.more(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const view_poject = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.view_poject(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("view_poject", locale);
    if (locale === "en")
        return en.view_poject(inputs);
    return fr.view_poject(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const share_project = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.share_project(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("share_project", locale);
    if (locale === "en")
        return en.share_project(inputs);
    return fr.share_project(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const delete_project = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.delete_project(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("delete_project", locale);
    if (locale === "en")
        return en.delete_project(inputs);
    return fr.delete_project(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const upgrade_to_premium = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.upgrade_to_premium(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("upgrade_to_premium", locale);
    if (locale === "en")
        return en.upgrade_to_premium(inputs);
    return fr.upgrade_to_premium(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("account", locale);
    if (locale === "en")
        return en.account(inputs);
    return fr.account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const billing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.billing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("billing", locale);
    if (locale === "en")
        return en.billing(inputs);
    return fr.billing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const log_out = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.log_out(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("log_out", locale);
    if (locale === "en")
        return en.log_out(inputs);
    return fr.log_out(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const knowledge_base = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.knowledge_base(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("knowledge_base", locale);
    if (locale === "en")
        return en.knowledge_base(inputs);
    return fr.knowledge_base(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const discover_comprehensive_guides_industry_insights = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.discover_comprehensive_guides_industry_insights(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("discover_comprehensive_guides_industry_insights", locale);
    if (locale === "en")
        return en.discover_comprehensive_guides_industry_insights(inputs);
    return fr.discover_comprehensive_guides_industry_insights(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const hackathon_profile = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.hackathon_profile(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("hackathon_profile", locale);
    if (locale === "en")
        return en.hackathon_profile(inputs);
    return fr.hackathon_profile(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const no_devpost_profile_information_available = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.no_devpost_profile_information_available(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("no_devpost_profile_information_available", locale);
    if (locale === "en")
        return en.no_devpost_profile_information_available(inputs);
    return fr.no_devpost_profile_information_available(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const website = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.website(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("website", locale);
    if (locale === "en")
        return en.website(inputs);
    return fr.website(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const wins = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.wins(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("wins", locale);
    if (locale === "en")
        return en.wins(inputs);
    return fr.wins(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const first_places = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.first_places(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("first_places", locale);
    if (locale === "en")
        return en.first_places(inputs);
    return fr.first_places(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const win_rate = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.win_rate(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("win_rate", locale);
    if (locale === "en")
        return en.win_rate(inputs);
    return fr.win_rate(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const skills = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.skills(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("skills", locale);
    if (locale === "en")
        return en.skills(inputs);
    return fr.skills(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const interests = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.interests(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("interests", locale);
    if (locale === "en")
        return en.interests(inputs);
    return fr.interests(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const recent_hackathons = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.recent_hackathons(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("recent_hackathons", locale);
    if (locale === "en")
        return en.recent_hackathons(inputs);
    return fr.recent_hackathons(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const members = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.members(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("members", locale);
    if (locale === "en")
        return en.members(inputs);
    return fr.members(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const team = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.team(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("team", locale);
    if (locale === "en")
        return en.team(inputs);
    return fr.team(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const view_code = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.view_code(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("view_code", locale);
    if (locale === "en")
        return en.view_code(inputs);
    return fr.view_code(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const yearly_participation = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.yearly_participation(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("yearly_participation", locale);
    if (locale === "en")
        return en.yearly_participation(inputs);
    return fr.yearly_participation(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const achievements = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.achievements(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("achievements", locale);
    if (locale === "en")
        return en.achievements(inputs);
    return fr.achievements(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const first_place_wins = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.first_place_wins(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("first_place_wins", locale);
    if (locale === "en")
        return en.first_place_wins(inputs);
    return fr.first_place_wins(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const category_prizes = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.category_prizes(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("category_prizes", locale);
    if (locale === "en")
        return en.category_prizes(inputs);
    return fr.category_prizes(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const github_profile1 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.github_profile1(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("github_profile1", locale);
    if (locale === "en")
        return en.github_profile1(inputs);
    return fr.github_profile1(inputs);
};
export { github_profile1 as "gitHub_profile" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const no_github_information_available1 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.no_github_information_available1(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("no_github_information_available1", locale);
    if (locale === "en")
        return en.no_github_information_available1(inputs);
    return fr.no_github_information_available1(inputs);
};
export { no_github_information_available1 as "no_gitHub_information_available" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const repositories = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.repositories(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("repositories", locale);
    if (locale === "en")
        return en.repositories(inputs);
    return fr.repositories(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const followers = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.followers(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("followers", locale);
    if (locale === "en")
        return en.followers(inputs);
    return fr.followers(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const longest_streak = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.longest_streak(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("longest_streak", locale);
    if (locale === "en")
        return en.longest_streak(inputs);
    return fr.longest_streak(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const languages_technologies = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.languages_technologies(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("languages_technologies", locale);
    if (locale === "en")
        return en.languages_technologies(inputs);
    return fr.languages_technologies(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const contribution_activity = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.contribution_activity(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("contribution_activity", locale);
    if (locale === "en")
        return en.contribution_activity(inputs);
    return fr.contribution_activity(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const most_active_day = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.most_active_day(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("most_active_day", locale);
    if (locale === "en")
        return en.most_active_day(inputs);
    return fr.most_active_day(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const weekly_average = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.weekly_average(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("weekly_average", locale);
    if (locale === "en")
        return en.weekly_average(inputs);
    return fr.weekly_average(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const current_streak = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.current_streak(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("current_streak", locale);
    if (locale === "en")
        return en.current_streak(inputs);
    return fr.current_streak(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const top_repositories = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.top_repositories(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("top_repositories", locale);
    if (locale === "en")
        return en.top_repositories(inputs);
    return fr.top_repositories(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const activity_metrics = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.activity_metrics(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("activity_metrics", locale);
    if (locale === "en")
        return en.activity_metrics(inputs);
    return fr.activity_metrics(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const contributions_day = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.contributions_day(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("contributions_day", locale);
    if (locale === "en")
        return en.contributions_day(inputs);
    return fr.contributions_day(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const active_days_per_week = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.active_days_per_week(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("active_days_per_week", locale);
    if (locale === "en")
        return en.active_days_per_week(inputs);
    return fr.active_days_per_week(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const loading_jobs = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.loading_jobs(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("loading_jobs", locale);
    if (locale === "en")
        return en.loading_jobs(inputs);
    return fr.loading_jobs(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const create_a_new_job = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.create_a_new_job(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("create_a_new_job", locale);
    if (locale === "en")
        return en.create_a_new_job(inputs);
    return fr.create_a_new_job(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const we_use = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.we_use(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("we_use", locale);
    if (locale === "en")
        return en.we_use(inputs);
    return fr.we_use(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const on_our_site_to_enhance_your_experience = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.on_our_site_to_enhance_your_experience(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("on_our_site_to_enhance_your_experience", locale);
    if (locale === "en")
        return en.on_our_site_to_enhance_your_experience(inputs);
    return fr.on_our_site_to_enhance_your_experience(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const allow_cookies = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.allow_cookies(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("allow_cookies", locale);
    if (locale === "en")
        return en.allow_cookies(inputs);
    return fr.allow_cookies(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const decline = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.decline(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("decline", locale);
    if (locale === "en")
        return en.decline(inputs);
    return fr.decline(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const its_not_you_its_us = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.its_not_you_its_us(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("its_not_you_its_us", locale);
    if (locale === "en")
        return en.its_not_you_its_us(inputs);
    return fr.its_not_you_its_us(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const something_went_wrong_on_our_end_please_try_again_later = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.something_went_wrong_on_our_end_please_try_again_later(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("something_went_wrong_on_our_end_please_try_again_later", locale);
    if (locale === "en")
        return en.something_went_wrong_on_our_end_please_try_again_later(inputs);
    return fr.something_went_wrong_on_our_end_please_try_again_later(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const drag_and_drop_your_file_here_or_click_to_browse = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.drag_and_drop_your_file_here_or_click_to_browse(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("drag_and_drop_your_file_here_or_click_to_browse", locale);
    if (locale === "en")
        return en.drag_and_drop_your_file_here_or_click_to_browse(inputs);
    return fr.drag_and_drop_your_file_here_or_click_to_browse(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const browse_files = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.browse_files(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("browse_files", locale);
    if (locale === "en")
        return en.browse_files(inputs);
    return fr.browse_files(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const remove = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.remove(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("remove", locale);
    if (locale === "en")
        return en.remove(inputs);
    return fr.remove(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const cancel = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.cancel(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("cancel", locale);
    if (locale === "en")
        return en.cancel(inputs);
    return fr.cancel(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const oops_this_page_was_not_found = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.oops_this_page_was_not_found(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("oops_this_page_was_not_found", locale);
    if (locale === "en")
        return en.oops_this_page_was_not_found(inputs);
    return fr.oops_this_page_was_not_found(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const return_home = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.return_home(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("return_home", locale);
    if (locale === "en")
        return en.return_home(inputs);
    return fr.return_home(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const loading = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.loading(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("loading", locale);
    if (locale === "en")
        return en.loading(inputs);
    return fr.loading(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const go_to_login = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.go_to_login(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("go_to_login", locale);
    if (locale === "en")
        return en.go_to_login(inputs);
    return fr.go_to_login(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const last_name = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.last_name(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("last_name", locale);
    if (locale === "en")
        return en.last_name(inputs);
    return fr.last_name(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const email = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.email(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("email", locale);
    if (locale === "en")
        return en.email(inputs);
    return fr.email(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const phone = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.phone(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("phone", locale);
    if (locale === "en")
        return en.phone(inputs);
    return fr.phone(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const already_have_an_account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.already_have_an_account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("already_have_an_account", locale);
    if (locale === "en")
        return en.already_have_an_account(inputs);
    return fr.already_have_an_account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const sign_in = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.sign_in(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("sign_in", locale);
    if (locale === "en")
        return en.sign_in(inputs);
    return fr.sign_in(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const welcome_back = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.welcome_back(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("welcome_back", locale);
    if (locale === "en")
        return en.welcome_back(inputs);
    return fr.welcome_back(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const login_to_your_account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.login_to_your_account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("login_to_your_account", locale);
    if (locale === "en")
        return en.login_to_your_account(inputs);
    return fr.login_to_your_account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const login = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.login(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("login", locale);
    if (locale === "en")
        return en.login(inputs);
    return fr.login(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const continue_with_sso = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.continue_with_sso(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("continue_with_sso", locale);
    if (locale === "en")
        return en.continue_with_sso(inputs);
    return fr.continue_with_sso(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const dont_have_an_account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.dont_have_an_account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("dont_have_an_account", locale);
    if (locale === "en")
        return en.dont_have_an_account(inputs);
    return fr.dont_have_an_account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const im_a_student_go_to_student_portal2 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.im_a_student_go_to_student_portal2(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("im_a_student_go_to_student_portal2", locale);
    if (locale === "en")
        return en.im_a_student_go_to_student_portal2(inputs);
    return fr.im_a_student_go_to_student_portal2(inputs);
};
export { im_a_student_go_to_student_portal2 as "im_a_Student_go_to_Student_portal" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const by_clicking_continue_you_agree_to_our = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.by_clicking_continue_you_agree_to_our(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("by_clicking_continue_you_agree_to_our", locale);
    if (locale === "en")
        return en.by_clicking_continue_you_agree_to_our(inputs);
    return fr.by_clicking_continue_you_agree_to_our(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const terms_of_service = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.terms_of_service(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("terms_of_service", locale);
    if (locale === "en")
        return en.terms_of_service(inputs);
    return fr.terms_of_service(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const and = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.and(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("and", locale);
    if (locale === "en")
        return en.and(inputs);
    return fr.and(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const privacy_policy = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.privacy_policy(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("privacy_policy", locale);
    if (locale === "en")
        return en.privacy_policy(inputs);
    return fr.privacy_policy(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const sign_up = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.sign_up(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("sign_up", locale);
    if (locale === "en")
        return en.sign_up(inputs);
    return fr.sign_up(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const login_to_your_organization_account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.login_to_your_organization_account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("login_to_your_organization_account", locale);
    if (locale === "en")
        return en.login_to_your_organization_account(inputs);
    return fr.login_to_your_organization_account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const not_using_sso_go_back = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.not_using_sso_go_back(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("not_using_sso_go_back", locale);
    if (locale === "en")
        return en.not_using_sso_go_back(inputs);
    return fr.not_using_sso_go_back(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const first_name = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.first_name(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("first_name", locale);
    if (locale === "en")
        return en.first_name(inputs);
    return fr.first_name(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const create_your_account = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.create_your_account(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("create_your_account", locale);
    if (locale === "en")
        return en.create_your_account(inputs);
    return fr.create_your_account(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const invalid_invite = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.invalid_invite(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("invalid_invite", locale);
    if (locale === "en")
        return en.invalid_invite(inputs);
    return fr.invalid_invite(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const about = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.about(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("about", locale);
    if (locale === "en")
        return en.about(inputs);
    return fr.about(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const pricing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.pricing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("pricing", locale);
    if (locale === "en")
        return en.pricing(inputs);
    return fr.pricing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const contact = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.contact(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("contact", locale);
    if (locale === "en")
        return en.contact(inputs);
    return fr.contact(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const toggle_menu = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.toggle_menu(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("toggle_menu", locale);
    if (locale === "en")
        return en.toggle_menu(inputs);
    return fr.toggle_menu(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const tailed_has_revolutionized_our_hiring_process = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.tailed_has_revolutionized_our_hiring_process(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("tailed_has_revolutionized_our_hiring_process", locale);
    if (locale === "en")
        return en.tailed_has_revolutionized_our_hiring_process(inputs);
    return fr.tailed_has_revolutionized_our_hiring_process(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const request_a_demo = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.request_a_demo(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("request_a_demo", locale);
    if (locale === "en")
        return en.request_a_demo(inputs);
    return fr.request_a_demo(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const the_automated_ranking_system_has_cut_our_screening_time = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.the_automated_ranking_system_has_cut_our_screening_time(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("the_automated_ranking_system_has_cut_our_screening_time", locale);
    if (locale === "en")
        return en.the_automated_ranking_system_has_cut_our_screening_time(inputs);
    return fr.the_automated_ranking_system_has_cut_our_screening_time(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const proven_impact_on_talent_acquisition = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.proven_impact_on_talent_acquisition(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("proven_impact_on_talent_acquisition", locale);
    if (locale === "en")
        return en.proven_impact_on_talent_acquisition(inputs);
    return fr.proven_impact_on_talent_acquisition(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const see_how_tailed_connects_companies_with_top_emerging_talent = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.see_how_tailed_connects_companies_with_top_emerging_talent(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("see_how_tailed_connects_companies_with_top_emerging_talent", locale);
    if (locale === "en")
        return en.see_how_tailed_connects_companies_with_top_emerging_talent(inputs);
    return fr.see_how_tailed_connects_companies_with_top_emerging_talent(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const first_of_all_a_huge_thank_you_to_tailed = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.first_of_all_a_huge_thank_you_to_tailed(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("first_of_all_a_huge_thank_you_to_tailed", locale);
    if (locale === "en")
        return en.first_of_all_a_huge_thank_you_to_tailed(inputs);
    return fr.first_of_all_a_huge_thank_you_to_tailed(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const just_knowing_theres_a_space_designed = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.just_knowing_theres_a_space_designed(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("just_knowing_theres_a_space_designed", locale);
    if (locale === "en")
        return en.just_knowing_theres_a_space_designed(inputs);
    return fr.just_knowing_theres_a_space_designed(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const honestly_tailed_offers_a_super_positive_and_modern_approach = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.honestly_tailed_offers_a_super_positive_and_modern_approach(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("honestly_tailed_offers_a_super_positive_and_modern_approach", locale);
    if (locale === "en")
        return en.honestly_tailed_offers_a_super_positive_and_modern_approach(inputs);
    return fr.honestly_tailed_offers_a_super_positive_and_modern_approach(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const software_engineering_student = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.software_engineering_student(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("software_engineering_student", locale);
    if (locale === "en")
        return en.software_engineering_student(inputs);
    return fr.software_engineering_student(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const company = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.company(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("company", locale);
    if (locale === "en")
        return en.company(inputs);
    return fr.company(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const our_origin = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.our_origin(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("our_origin", locale);
    if (locale === "en")
        return en.our_origin(inputs);
    return fr.our_origin(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const learn_more_about_our_mission_vision_and_values = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.learn_more_about_our_mission_vision_and_values(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("learn_more_about_our_mission_vision_and_values", locale);
    if (locale === "en")
        return en.learn_more_about_our_mission_vision_and_values(inputs);
    return fr.learn_more_about_our_mission_vision_and_values(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const mission_vision_and_value = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.mission_vision_and_value(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("mission_vision_and_value", locale);
    if (locale === "en")
        return en.mission_vision_and_value(inputs);
    return fr.mission_vision_and_value(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const where_francks_vision_met_purpose_tailed_was_born_to_redefine_hiring = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.where_francks_vision_met_purpose_tailed_was_born_to_redefine_hiring(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("where_francks_vision_met_purpose_tailed_was_born_to_redefine_hiring", locale);
    if (locale === "en")
        return en.where_francks_vision_met_purpose_tailed_was_born_to_redefine_hiring(inputs);
    return fr.where_francks_vision_met_purpose_tailed_was_born_to_redefine_hiring(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const our_team = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.our_team(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("our_team", locale);
    if (locale === "en")
        return en.our_team(inputs);
    return fr.our_team(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const meet_our_team = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.meet_our_team(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("meet_our_team", locale);
    if (locale === "en")
        return en.meet_our_team(inputs);
    return fr.meet_our_team(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const product = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.product(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("product", locale);
    if (locale === "en")
        return en.product(inputs);
    return fr.product(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const features = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.features(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("features", locale);
    if (locale === "en")
        return en.features(inputs);
    return fr.features(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const origin = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.origin(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("origin", locale);
    if (locale === "en")
        return en.origin(inputs);
    return fr.origin(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const careers = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.careers(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("careers", locale);
    if (locale === "en")
        return en.careers(inputs);
    return fr.careers(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const sustainability = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.sustainability(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("sustainability", locale);
    if (locale === "en")
        return en.sustainability(inputs);
    return fr.sustainability(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const terms_conditions = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.terms_conditions(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("terms_conditions", locale);
    if (locale === "en")
        return en.terms_conditions(inputs);
    return fr.terms_conditions(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
const tailed_all_rights_reserved1 = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.tailed_all_rights_reserved1(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("tailed_all_rights_reserved1", locale);
    if (locale === "en")
        return en.tailed_all_rights_reserved1(inputs);
    return fr.tailed_all_rights_reserved1(inputs);
};
export { tailed_all_rights_reserved1 as "Tailed_all_rights_reserved" };
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const legal = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.legal(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("legal", locale);
    if (locale === "en")
        return en.legal(inputs);
    return fr.legal(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const coming_soon = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.coming_soon(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("coming_soon", locale);
    if (locale === "en")
        return en.coming_soon(inputs);
    return fr.coming_soon(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const were_working_hard_to_bring_you_something_amazing = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.were_working_hard_to_bring_you_something_amazing(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("were_working_hard_to_bring_you_something_amazing", locale);
    if (locale === "en")
        return en.were_working_hard_to_bring_you_something_amazing(inputs);
    return fr.were_working_hard_to_bring_you_something_amazing(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const he_had_been_filled_with_curiosity_since_childhood = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.he_had_been_filled_with_curiosity_since_childhood(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("he_had_been_filled_with_curiosity_since_childhood", locale);
    if (locale === "en")
        return en.he_had_been_filled_with_curiosity_since_childhood(inputs);
    return fr.he_had_been_filled_with_curiosity_since_childhood(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const eventually_he_earned_a_specialized_degree_in_computer_science = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.eventually_he_earned_a_specialized_degree_in_computer_science(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("eventually_he_earned_a_specialized_degree_in_computer_science", locale);
    if (locale === "en")
        return en.eventually_he_earned_a_specialized_degree_in_computer_science(inputs);
    return fr.eventually_he_earned_a_specialized_degree_in_computer_science(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const however_in_his_new_home_his_degree_was_not_recognized = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.however_in_his_new_home_his_degree_was_not_recognized(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("however_in_his_new_home_his_degree_was_not_recognized", locale);
    if (locale === "en")
        return en.however_in_his_new_home_his_degree_was_not_recognized(inputs);
    return fr.however_in_his_new_home_his_degree_was_not_recognized(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const he_was_not_alone_in_the_bustling_atmosphere_of_hackathons = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.he_was_not_alone_in_the_bustling_atmosphere_of_hackathons(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("he_was_not_alone_in_the_bustling_atmosphere_of_hackathons", locale);
    if (locale === "en")
        return en.he_was_not_alone_in_the_bustling_atmosphere_of_hackathons(inputs);
    return fr.he_was_not_alone_in_the_bustling_atmosphere_of_hackathons(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const the_process_felt_like_a_story_unfolding_yet_reality_was_harsh = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.the_process_felt_like_a_story_unfolding_yet_reality_was_harsh(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("the_process_felt_like_a_story_unfolding_yet_reality_was_harsh", locale);
    if (locale === "en")
        return en.the_process_felt_like_a_story_unfolding_yet_reality_was_harsh(inputs);
    return fr.the_process_felt_like_a_story_unfolding_yet_reality_was_harsh(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const mission_to_empower_the_next_generation_of_tech_talent = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.mission_to_empower_the_next_generation_of_tech_talent(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("mission_to_empower_the_next_generation_of_tech_talent", locale);
    if (locale === "en")
        return en.mission_to_empower_the_next_generation_of_tech_talent(inputs);
    return fr.mission_to_empower_the_next_generation_of_tech_talent(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const the_birth_of_tailed = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.the_birth_of_tailed(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("the_birth_of_tailed", locale);
    if (locale === "en")
        return en.the_birth_of_tailed(inputs);
    return fr.the_birth_of_tailed(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const the_challenge_of_recruitment_was_not_limited_to_students_alone = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.the_challenge_of_recruitment_was_not_limited_to_students_alone(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("the_challenge_of_recruitment_was_not_limited_to_students_alone", locale);
    if (locale === "en")
        return en.the_challenge_of_recruitment_was_not_limited_to_students_alone(inputs);
    return fr.the_challenge_of_recruitment_was_not_limited_to_students_alone(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const under_the_current_system_internships_were_granted_based_on_academic_credentials = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.under_the_current_system_internships_were_granted_based_on_academic_credentials(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("under_the_current_system_internships_were_granted_based_on_academic_credentials", locale);
    if (locale === "en")
        return en.under_the_current_system_internships_were_granted_based_on_academic_credentials(inputs);
    return fr.under_the_current_system_internships_were_granted_based_on_academic_credentials(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const a_resume_filled_with_academic_achievements_alone_could_not_capture_a_persons_true_potential = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.a_resume_filled_with_academic_achievements_alone_could_not_capture_a_persons_true_potential(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("a_resume_filled_with_academic_achievements_alone_could_not_capture_a_persons_true_potential", locale);
    if (locale === "en")
        return en.a_resume_filled_with_academic_achievements_alone_could_not_capture_a_persons_true_potential(inputs);
    return fr.a_resume_filled_with_academic_achievements_alone_could_not_capture_a_persons_true_potential(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const that_is_when_franck_conceived_the_idea_of_tailed = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.that_is_when_franck_conceived_the_idea_of_tailed(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("that_is_when_franck_conceived_the_idea_of_tailed", locale);
    if (locale === "en")
        return en.that_is_when_franck_conceived_the_idea_of_tailed(inputs);
    return fr.that_is_when_franck_conceived_the_idea_of_tailed(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const talent_should_not_be_measured_by_academic_degrees = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.talent_should_not_be_measured_by_academic_degrees(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("talent_should_not_be_measured_by_academic_degrees", locale);
    if (locale === "en")
        return en.talent_should_not_be_measured_by_academic_degrees(inputs);
    return fr.talent_should_not_be_measured_by_academic_degrees(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const current = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.current(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("current", locale);
    if (locale === "en")
        return en.current(inputs);
    return fr.current(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const six_three_dollars_per_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.six_three_dollars_per_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("six_three_dollars_per_month", locale);
    if (locale === "en")
        return en.six_three_dollars_per_month(inputs);
    return fr.six_three_dollars_per_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const seven_dollars_per_employee_per_month = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.seven_dollars_per_employee_per_month(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("seven_dollars_per_employee_per_month", locale);
    if (locale === "en")
        return en.seven_dollars_per_employee_per_month(inputs);
    return fr.seven_dollars_per_employee_per_month(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const for_startups_with_one_to_nine_employees = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.for_startups_with_one_to_nine_employees(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("for_startups_with_one_to_nine_employees", locale);
    if (locale === "en")
        return en.for_startups_with_one_to_nine_employees(inputs);
    return fr.for_startups_with_one_to_nine_employees(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const discounted_pricing_support = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.discounted_pricing_support(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("discounted_pricing_support", locale);
    if (locale === "en")
        return en.discounted_pricing_support(inputs);
    return fr.discounted_pricing_support(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const giving_back_startup = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.giving_back_startup(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("giving_back_startup", locale);
    if (locale === "en")
        return en.giving_back_startup(inputs);
    return fr.giving_back_startup(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const growing_teams = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.growing_teams(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("growing_teams", locale);
    if (locale === "en")
        return en.growing_teams(inputs);
    return fr.growing_teams(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const catalog = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.catalog(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("catalog", locale);
    if (locale === "en")
        return en.catalog(inputs);
    return fr.catalog(inputs);
};
/**
* This function has been compiled by [Paraglide JS](https://inlang.com/m/gerre34r).
*
* - Changing this function will be over-written by the next build.
*
* - If you want to change the translations, you can either edit the source files e.g. `en.json`, or
* use another inlang app like [Fink](https://inlang.com/m/tdozzpar) or the [VSCode extension Sherlock](https://inlang.com/m/r7kp499g).
*
* @param {{}} inputs
* @param {{ locale?: "en" | "fr" }} options
* @returns {string}
*/
/* @__NO_SIDE_EFFECTS__ */
export const start_exploring_our_product_catalog_now = (inputs = {}, options = {}) => {
    var _a;
    if (experimentalMiddlewareLocaleSplitting && isServer === false) {
        return /** @type {any} */ (globalThis).__paraglide_ssr.start_exploring_our_product_catalog_now(inputs);
    }
    const locale = (_a = options.locale) !== null && _a !== void 0 ? _a : getLocale();
    trackMessageCall("start_exploring_our_product_catalog_now", locale);
    if (locale === "en")
        return en.start_exploring_our_product_catalog_now(inputs);
    return fr.start_exploring_our_product_catalog_now(inputs);
};
//# sourceMappingURL=_index.js.map